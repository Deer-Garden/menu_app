<header class="site-header">
      <div class="wrapper site-header__wrapper">
        <a href="#" class="brand">MenuApp</a>
        <nav class="nav">
          <ul class="nav__wrapper">
            <% if user_signed_in? %>
                <li><%= link_to current_user.nickname, main_menu_path(current_user.id), class: "nav-item" %></li> 
                <li><%= link_to '新規投稿', new_main_menu_path, class: "nav-item" %></li>
                <li><%= link_to 'サインアウト',  destroy_user_session_path, method: :delete, class: "nav-item" %></li>
            <% else %>
                <li><%= link_to 'サインイン', new_user_session_path, class: "nav-item" %></li>
                <li><%= link_to '新規登録', new_user_registration_path, class: "nav-item" %></li>
            <% end %>
          </ul>
        </nav>
      </div>
    </header>
